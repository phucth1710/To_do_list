<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List</title>
    <link rel="stylesheet" type="text/css" href="styles.css">
</head>
<body>
    <div class="box"  id="heading">
        <h1>Today is <%= day %></h1>
    </div>
    
    <div class="box" >
    <% for (var i = 0; i < toDoList.length; i++ ) { %>
        <form action="/delete" method="post">
            <div class="item">
                <input type="checkbox" onChange="this.form.submit()" name = "checkbox" value = "<%= toDoList[i]._id %>">
                <p class="listItem"> <%= toDoList[i].name %> </p>
                <input type="hidden" name="listName" value = "<%= listTitle %>"> 
            </div>  
        </form>
        <% } %>

    <form class="item" action="/" method="post" autocomplete="off">
        <input type="text" name="newItem" placeholder="Add New Item" autofocus autocomplete="off">

        <button type="submit" class="btn" name="list" value="<%= listTitle %>">+</button>
        <button type="button" class="btn clear" name="list" value="removeItem">x</button>
    </form>
</div>
</body>
</html>